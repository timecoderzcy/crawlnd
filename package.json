{
  "name": "crawlnd-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Crawlnd — Node.js TypeScript crawler runtime (monorepo)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w crawlnd && npm run build -w @crawlnd/utils && npm run build -w @crawlnd/admin",
    "build:packages": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/jsdom": "^28.0.3",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  },
  "license": "ISC",
  "engines": {
    "node": ">=20"
  }
}
